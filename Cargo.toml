[package]
name = "mandelbrot-drift"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = ["simd"]
simd = []

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
